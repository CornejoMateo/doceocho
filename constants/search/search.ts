/** Milliseconds to wait after the last keystroke before querying. */
export const SEARCH_DEBOUNCE_MS = 250;

/** Shortest term worth querying the database for. */
export const MIN_SEARCH_LENGTH = 2;

/** Rows each provider asks the database for. */
export const RESULTS_PER_PROVIDER = 5;

/** Key that opens the palette, combined with the platform modifier. */
export const SEARCH_SHORTCUT_KEY = 'k';
